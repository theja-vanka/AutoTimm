"""Flow CLI — operational commands for NightFlow desktop app.

These subcommands handle system metrics, augmentation previews,
HuggingFace Hub uploads, and TensorRT conversion. They are designed
to be invoked by the NightFlow Tauri backend (or directly from the
command line) and communicate results via JSON on stdout.
"""

from __future__ import annotations

__all__: list[str] = []
